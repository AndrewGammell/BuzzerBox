<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="io.buzzerbox.app" >

    <uses-permission android:name="android.permission.INTERNET" />

<application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        >
    <activity
        android:name="activities.MainActivity"
        android:windowSoftInputMode="adjustResize"
        android:label="@string/app_name"
        android:theme="@style/MyCustomThemeNoBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name="activities.PageViewActivity"
              android:label="BuzzerBox"
              android:theme="@style/MyCustomTheme">

    </activity>
    <activity android:name="activities.DisplayActivity"
              android:label="BuzzerBox"
              android:theme="@style/MyCustomThemeNoBar">

    </activity>
    <activity android:name="activities.SettingsPageActivity"
              android:label="BuzzerBox"
              android:theme="@style/MyCustomThemeNoBar">

    </activity>
</application>

</manifest>